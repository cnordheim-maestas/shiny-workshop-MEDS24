# load packages ----
library(shiny)

# user interface ----
ui <- fluidPage()

# server ----
server <- function(inputs, outputs) {}

# combine ui and server into app ----
shinyApp(ui = ui, server = server)
